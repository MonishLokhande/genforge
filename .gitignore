# Python
__pycache__/
*.py[oc]
build/
dist/
wheels/
*.egg-info/

# Virtual environments
.venv*/

# Lockfile (not shipped — consumers resolve their own)
uv.lock

# Caches
.pytest_cache/
.ruff_cache/
.cache/

# Run artifacts
checkpoints/
output/
outputs/
wandb/
data/

# MkDocs build (docs/ source IS shipped; the built site deploys via `mkdocs gh-deploy`)
site/
